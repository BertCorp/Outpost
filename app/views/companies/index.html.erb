<%= link_to 'Add New Company', new_company_path, class: "btn btn-primary pull-right" %>
<h1>All Companies</h1>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th># of Tests</th>
			<th># of Reports</th>
      <th>Current Status</th>
      <th>Most Recent</th>
      <th>Initial Suite</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @companies.each do |company| %>
    <tr>
      <td><%= link_to company.name, company %></td>
      <td><%= company.test_cases.count %></td>
			<td><%= company.reports.count %></td>
      <td><%= company.status %></td>
      <td>To Do</td><!-- What is this for? -->
      <td>To Do</td><!-- What is this for? -->
      <td><%= link_to 'Edit', edit_company_path(company), class: 'btn btn-primary btn-xs' %></td>
    </tr>
    <% end %>
		<% if @companies.count < 1 %>
		<tr>
			<td colspan="6" style="text-align: center;">No companies yet. <%= link_to "Add one", new_company_path %>.</td>
		</tr>
		<% end %>
  </tbody>
</table>
